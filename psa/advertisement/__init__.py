"""psa.advertisement — advertisement forgetting (pattern-level card decay).

Distinct from psa.forgetting (memory-level forgetting). Both are "forgetting"
mechanisms philosophically, but they operate on different cognitive surfaces:
psa.forgetting prunes memories; psa.advertisement prunes the advertisement
patterns on anchor cards.
"""
